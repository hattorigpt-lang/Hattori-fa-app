# FIRE & 資産運用 ライフイベントシミュレーター

資産推移・FIRE達成年齢・老後の取り崩し可能額を、ライフイベントとNISA・売却時課税まで織り込んで試算するブラウザアプリケーションです。

---

## 起動方法

ES Modules を使用しているため、`file://` での直接オープンでは CORS により動作しません。ローカルサーバー経由で開いてください。

```bash
# いずれかの方法で起動
python3 -m http.server 8000
npx http-server -p 8000
```

ブラウザで <http://localhost:8000/> を開きます。

---

## ディレクトリ構成

```
.
├── index.html              マークアップ（ロジックを含まない）
├── css/
│   └── style.css           デザイントークンとコンポーネントCSS
├── js/
│   ├── main.js             エントリポイント。各モジュールの配線のみ
│   ├── config.js           定数・初期値・入力の許容範囲
│   ├── state.js            単一状態ストア（購読／バリデーション）
│   ├── persistence.js      localStorage 保存とURL共有
│   ├── formatters.js       表示用の整形関数
│   │
│   ├── simulator.js        計算エンジン本体（DOM非依存の純粋関数）
│   ├── housing.js          住居費・住宅ローン・繰り上げ返済のモデル
│   ├── pension.js          公的年金の受給見込みの概算
│   ├── montecarlo.js       モンテカルロ・シミュレーション
│   ├── sensitivity.js      感度分析と目標逆算
│   ├── comparison.js       賃貸 vs 購入の並列比較
│   │
│   └── ui/
│       ├── dom.js          DOM操作ヘルパー
│       ├── inputs.js       入力フォームのバインディング・年金推計の表示
│       ├── events.js       ライフイベント表（差分レンダリング）
│       ├── housing.js      住居費セクションの表示制御とローン概要
│       ├── kpi.js          サマリー・KPIカード・警告
│       ├── tables.js       シナリオ比較表・年次明細
│       ├── comparison.js   賃貸 vs 購入 比較カード
│       ├── sensitivity.js  トルネードチャートと目標逆算の描画
│       ├── montecarlo.js   モンテカルロ結果とシーケンスリスクの描画
│       └── chart.js        Chart.js ラッパーと注釈描画
└── tools/
    └── verify.mjs          計算エンジンの回帰検証（Node）
```

### 設計方針

| 方針 | 内容 |
|---|---|
| 計算とDOMの分離 | `simulator.js` は DOM に一切依存せず、入力オブジェクトから結果オブジェクトを返す純粋関数のみで構成。Node からそのまま検証できます。 |
| 単一状態ストア | 状態の書き換えは `state.js` の `patch()` / `replace()` 経由のみ。グローバル変数への直接代入を排除しています。 |
| 宣言的なバインディング | HTML 側の `data-field="キー名"` が唯一の接点。要素IDを個別にキャッシュする必要がありません。 |
| 差分レンダリング | ライフイベント表は行を再利用し、グラフは `destroy()` せず `update('none')` で差分更新します。 |
| 定義の一元化 | 入力の許容範囲・FIRE目標タイプ・シナリオ定義は `config.js` に集約し、HTML と JS の二重管理を避けています。 |

---

## 検証

```bash
# 計算エンジンの回帰検証（20項目）
node tools/verify.mjs

# 静的解析
npx eslint js tools
```

`tools/verify.mjs` は、売却時課税モデルの手計算突合、シナリオ間の単調性、資産枯渇の検出、住宅ローンの償却計算、極端な入力での堅牢性など **29項目** を検査します。

計算エンジンが DOM に依存しないため、ブラウザを起動せずに数値の妥当性を確認できます。

---

## 計算モデルの前提

### 資産バケット

| バケット | 利回り | 課税 |
|---|---|---|
| 現金・預貯金 | 0%（固定） | なし |
| NISA口座 | 株式と同率 | 非課税（生涯枠1,800万・年間枠360万） |
| 株式・投信（課税口座） | 入力値 | 売却時に含み益へ 20.315% |
| その他運用資産 | 入力値 | 売却時に含み益へ 20.315% |

### 主要ルール（資産運用）

- **課税は売却時のみ。** 運用中は非課税で複利が働き、取り崩した金額のうち含み益に相当する部分にのみ課税します。取得原価は売却比率で按分して減額します。
- **積立順序** は NISA枠 →（枠を使い切ったら）課税口座。
- **取り崩し順序** は 現金 → 課税口座 → その他 → NISA。非課税の複利を最後まで温存する順序です。
- **FIRE達成判定** は、含み益への将来の税負担を差し引いた「税引後の実質資産」で行います。
- **シナリオ比較** は市況全体の想定として、リスク資産の利回りを一律に ±2 ポイントシフトさせます（標準＝入力値）。
- **「リタイア後に使える月額」** は、年金・インフレ・ライフイベント・売却時課税をすべて織り込んだうえで、想定寿命の時点で資産がちょうど尽きる水準を二分探索で逆算した値です。今日の物価に換算して表示します。

### 世帯（配偶者の合算）

収入と年金は「世帯メンバーの配列」として扱い、就労終了と年金受給開始を**各人の年齢**で判定します。夫婦に年齢差がある場合、配偶者が先に65歳になれば年金だけが先行して開始します。資産・支出は世帯単位で1組として扱います。

### 公的年金の自動推計

手取り年収と就職年齢から受給見込みを概算します。

- 老齢基礎年金 ＝ 満額 81.6万円 × 納付月数 / 480
- 老齢厚生年金 ＝ 平均標準報酬額 × 5.481/1000 × 加入月数（標準報酬月額の上限 65万円）
- 手取り入力から額面を逆算し、生涯平均の割引係数と年金の手取り率を掛けて調整
- 推計値は状態を書き換えず表示のみ差し替えます（自動推計をOFFに戻したとき、手入力していた値が失われないようにするため）
- 第3号被保険者・在職老齢年金・繰上げ／繰下げ受給・マクロ経済スライドは考慮していません

### 住居費・住宅ローン

住居費は「月間固定費」とは独立した支出ストリームとして扱います。利用者には固定費から家賃を除いてもらうことで二重計上を防ぎます（該当しそうな場合はUIが警告し、ワンクリックで差し引けます）。

- 購入までは家賃、購入後は「ローン返済＋維持費」を計上します。購入しない設定なら家賃が生涯続きます。
- 物件価格・頭金・諸費用は購入年までインフレで上昇します。一方、**ローン返済額は契約時に固定される名目額**のため以後は上昇しません（インフレ下で実質負担が軽くなる効果を再現）。
- 返済は元利均等。月次で償却スケジュールを計算し、住宅ローン控除の算定に用います。
- **住宅ローン控除**は年末残高（借入限度額3,000万円まで）× 0.7% を13年間、収入として加算します。所得税額を上限とする制度上の制約は考慮していません。
- **購入した住宅は金融資産に含めません。** 売却して生活費に充てられる資産ではなく、含めるとFIRE達成を過大評価するためです。
- FIRE目標額の基準となる生活費には、住居費の**経常分**（家賃 or ローン返済＋維持費）を含めます。頭金などの一時費用は含めません。

#### 繰り上げ返済

各年の年末にまとめて実行するものとして償却します。

- **期間短縮型**: 毎月の返済額を据え置いて完済を早めます。利息の削減効果が最大になります。
- **返済額軽減型**: 完済時期を据え置き、残存期間で返済額を再計算します。月々の負担が下がります。
- 返済しない場合の償却も同時に計算し、利息の削減額と短縮年数を提示します。

#### 賃貸 vs 購入の比較

住宅の設定だけを入れ替えた2本の試算を並べ、FIRE達成年齢・寿命時点の資産（税引後）・生涯の住居費を比較します。判断の主軸は**終端資産の差**です。FIRE達成年齢だけを見ると、ローン返済中の一時的な支出増で購入が不当に不利に見えるためです。

入力のたびに実行されるため、取り崩し可能額の二分探索を伴う `runSimulation` は使わず、標準シナリオの年次プロジェクションだけを回す軽量経路（約5ms）で計算しています。

### モンテカルロ・シミュレーション

決定論モデル（毎年きっちり x% で運用）では、平均リターンが同じでも暴落がリタイア前後に来ると資産寿命が大きく縮む「シーケンス・オブ・リターン・リスク」を表現できません。

- 年次リターンを**対数正規分布**から抽選します。正規分布では -100% を下回るリターンが発生しうるためです。
- 算術平均とボラティリティから対数正規のパラメータを導出し、期待リターンが入力値と一致するよう補正しています。
- 乱数は**シード固定**です。入力を変えていないのに結果が揺れると比較できなくなるため、再現性を優先しています。
- FIRE目標額の算定には**期待利回り**を使い続けます（目標が乱数で揺れないようにするため）。
- 出力: FIRE達成率、資産枯渇率、達成年齢の10/50/90パーセンタイル、終端資産の分位、年次資産の10〜90%レンジ。

#### シーケンス・オブ・リターン・リスク

各試行の「リタイア直後5年の年率リターン」で全試行を並べ替え、下位25%・全体・上位25%それぞれの資産枯渇率を比較します。**平均リターンが同じでも、リタイア直後が不調だと枯渇確率が数倍に跳ね上がる**ことが数値で確認できます。

### 感度分析

各変数を単独で動かしたときの FIRE 達成年の変化を、影響の大きい順に並べます。金額系は ±10%、率系は ±1 パーセントポイントで変動させます。

資産曲線と目標額曲線の交点を**線形補間して小数年**で評価しています。年単位の丸めでは「現在の総資産 ±10%」のような小さなレバーの効果が 0年 に潰れて比較できなくなるためです。

#### 目標逆算（ゴールシーク）

「FIRE達成を N 年早めるには、各変数をいくらにすればよいか」を二分探索で逆算します。感度分析が傾きを示すのに対し、こちらは目標に必要な水準を示すため、そのまま行動計画になります。影響の大きい上位5項目に絞って計算し、入力の応答性を保つため320msの遅延サイクルで実行します。

---

## 印刷 / PDF出力

ヘッダーの「印刷 / PDF」から、提案書として使えるA4レイアウトで出力できます。操作用の要素を落として1カラムに組み直し、カードの途中でページが割れないよう `break-inside` を指定しています。印刷用の見出しには、世帯構成・年収・支出・総資産・FIRE達成予想を1行に集約しています。

年次キャッシュフロー明細は、画面で展開してから印刷した場合のみ出力されます。

---

## データの取り扱い

入力内容はブラウザの `localStorage` にのみ保存され、外部へ送信されません。「条件を共有」で生成されるURLは、状態を base64url へエンコードしてハッシュ部（`#`以降）に格納するため、サーバーへは送信されません。

---

## 外部依存

| 依存 | 用途 | 読み込み |
|---|---|---|
| Chart.js 4.4.1 | グラフ描画 | jsDelivr CDN（読み込めない場合は代替メッセージを表示） |
| Google Fonts (Inter / Noto Sans JP) | 書体 | Google Fonts CDN（フォールバック指定あり） |

---

## 免責

本ツールは概算であり、将来の運用成果・税制・社会保障制度を保証するものではありません。投資判断はご自身の責任でお願いします。
